"""
Write a Python program which accepts the radius of a circle from the user and computes the area.
"""
from math import pi
rad = input ("Input the radius of the circle : ")
r = float(rad)
print ("The area of the circle with radius " + str(r) + " is: " + str(pi * r**2))
